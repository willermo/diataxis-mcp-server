# Get started with Widget CLI

In this tutorial, you will create your first widget project.

1. Install the CLI.
2. Run `widget init`.
3. Start the development server.

You should see a local URL in the terminal.

Notice that the starter project includes one widget file.
